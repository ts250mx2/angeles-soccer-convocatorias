import ConvocatoriasPantalla from '@/components/ConvocatoriasPantalla';

/** Solo las ligas. La pantalla es la misma; cambia el corte. */
export default function LigasPage() {
    return <ConvocatoriasPantalla tipo="liga" />;
}
